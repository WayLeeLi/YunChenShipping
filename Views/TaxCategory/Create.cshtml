@model YunChenShipping.Models.TaxCategory
@{
    ViewData["Title"] = "系統管理 / 新增營業稅";
}

<form asp-action="Create" method="post">
    <div class="form-card">
        <div class="form-card-header">
            <i class="bi bi-plus-circle"></i> 新增營業稅
        </div>
        <div class="form-card-body">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="row mb-2 align-items-center">
                <label class="form-label-custom"><span class="required">●</span> 營業稅名稱</label>
                <div class="col"><input asp-for="Name" class="form-control" placeholder="例如：一般稅、零稅、免稅" /><span asp-validation-for="Name" class="text-danger small"></span></div>
            </div>

            <div class="row mb-2 align-items-center">
                <label class="form-label-custom"><span class="required">●</span> 稅率(%)</label>
                <div class="col"><input asp-for="TaxRate" class="form-control" type="number" step="0.01" min="0" max="100" placeholder="例如：5" /><span asp-validation-for="TaxRate" class="text-danger small"></span></div>
            </div>

            <div class="row mb-2 align-items-center">
                <label class="form-label-custom">排序</label>
                <div class="col"><input asp-for="SortOrder" class="form-control" type="number" value="0" /><span asp-validation-for="SortOrder" class="text-danger small"></span></div>
            </div>
        </div>
    </div>

    <div class="d-flex justify-content-center gap-3 mt-4">
        <a asp-action="Index" class="btn btn-secondary px-4"><i class="bi bi-arrow-left"></i> 回上一頁</a>
        <button type="submit" class="btn btn-primary px-4"><i class="bi bi-check-lg"></i> 確定新增</button>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
